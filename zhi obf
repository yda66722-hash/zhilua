--[[ 
    纸脚本 - 25ms 报错伪装虚拟机 v14.0
    特性：虚假报错注入、fenv 环境干扰、VM 多态分段
]]

local function GetUIContainer()
    local success, res = pcall(function() return game:GetService("CoreGui") end)
    return success and res or game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")
end

local function ObfuscateProcess(source)
    -- 1. 【25ms 风格伪装】直接注入到生成的代码最顶层
local fenv = getfenv()
local _ = fenv._ENV
task.spawn(function() 
    warn("ex Power by zhi obf")
end)

    -- 2. 核心防御 (物理自毁)
    local anti = "if pcall(function()return game:IsA(nil)end)then error('line 1: gng wtf')end;"
    local fullSource = anti .. source
    
    -- 3. 物理分段逻辑
    local segments = {}
    local i = 1
    while i <= #fullSource do
        local len = math.random(5, 8)
        table.insert(segments, string.sub(fullSource, i, i + len - 1))
        i = i + len
    end
    
    -- 4. VM 指令流
    local opcodes = {}
    for _, s in ipairs(segments) do
        local mode = math.random(1, 2)
        local t = {}
        if mode == 1 then
            local k = math.random(10, 40)
            for j=1, #s do table.insert(t, s:byte(j) + k) end
            table.insert(opcodes, "{m=1,k="..k..",d={"..table.concat(t, ",").."}}")
        else
            for j=1, #s do table.insert(t, s:byte(j)) end
            table.insert(opcodes, "{m=2,d={"..table.concat(t, ",").."}}")
        end
    end

    local bytecode = "{" .. table.concat(opcodes, ",") .. "}"

    -- 5. 终极还原模板 (注入 25ms 混淆信息)
    local template = [[%s
local _s = %s
local _vm = function(dat)
    local _r = {}
    for i, v in ipairs(dat) do
        local str = ""
        if v.m == 1 then
            for _, b in ipairs(v.d) do str = str .. string.char(b - v.k) end
        else
            for _, b in ipairs(v.d) do str = str .. string.char(b) end
        end
        _r[i] = str
    end
    local f, err = loadstring(table.concat(_r))
    if f then f() end
end
_vm(_s)]]
    return string.format(template, fakeError, bytecode)
end

-- --- UI 界面 ---
local container = GetUIContainer()
if container:FindFirstChild("Paper_25ms_Fake") then container["Paper_25ms_Fake"]:Destroy() end

local ScreenGui = Instance.new("ScreenGui", container)
ScreenGui.Name = "Paper_25ms_Fake"

local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 420, 0, 380)
Main.Position = UDim2.new(0.5, -210, 0.5, -190)
Main.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Main.Active = true
Main.Draggable = true
Instance.new("UICorner", Main)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
Title.Text = "纸团队 - 25ms 报错伪装版"
Title.TextColor3 = Color3.fromRGB(255, 50, 50)
Title.TextSize = 18
Title.Parent = Main

local Box = Instance.new("TextBox", Main)
Box.Size = UDim2.new(0, 400, 0, 220)
Box.Position = UDim2.new(0, 10, 0, 60)
Box.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
Box.TextColor3 = Color3.new(1, 1, 1)
Box.Text = ""
Box.PlaceholderText = "粘贴 25ms 风格代码..."
Box.MultiLine = true
Box.Parent = Main

local Btn = Instance.new("TextButton", Main)
Btn.Size = UDim2.new(0, 400, 0, 60)
Btn.Position = UDim2.new(0, 10, 0, 300)
Btn.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
Btn.Text = "生成【25ms 报错伪装脚本】"
Btn.TextColor3 = Color3.new(1, 1, 1)
Btn.TextSize = 20
Btn.Parent = Main
Instance.new("UICorner", Btn)

Btn.MouseButton1Click:Connect(function()
    if #Box.Text < 5 then return end
    local ok, res = pcall(function() return ObfuscateProcess(Box.Text) end)
    if ok then
        setclipboard(res)
        Box.Text = res
        Btn.Text = "伪装脚本已生成并复制！"
        Btn.BackgroundColor3 = Color3.fromRGB(0, 150, 80)
    end
end)